# --- Document Ingestion & Image Handling ---
PyMuPDF==1.24.9
python-docx==1.1.2
pdf2image==1.17.0
Pillow==10.4.0
pytesseract==0.3.13
opencv-python==4.10.0.84

# --- Deep Learning & VLM (GPU-Accelerated) ---
# NOTE: torch must be installed separately BEFORE this file, with the CUDA build matching your GPU:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
torch>=2.9.0
# torchvision is required by timm (Florence-2's vision backbone) — install alongside torch
# from the same CUDA-specific index: pip install torchvision --index-url https://download.pytorch.org/whl/cu128
torchvision>=0.17.0
# Pinned exactly — transformers 5.0+ breaks Florence-2's remote code
# (AttributeError: 'Florence2LanguageConfig' object has no attribute 'forced_bos_token_id').
# Do not use >= here until Microsoft updates Florence-2's config code for transformers 5.x.
transformers==4.51.3
accelerate>=0.33.0
einops>=0.8.0
sentence-transformers==3.0.1
timm>=0.9.8

# --- NLP Preprocessing ---
spacy==3.7.6

# --- Evaluation, ML & Data Handling ---
scikit-learn==1.5.1
pandas==2.2.2
numpy==1.26.4

# --- Visualization & UI ---
matplotlib==3.9.1.post1
seaborn==0.13.2
wordcloud==1.9.3
streamlit==1.37.1

# --- Config & Utilities ---
PyYAML==6.0.2
python-dotenv==1.0.1
tqdm==4.66.4
requests>=2.32.0
